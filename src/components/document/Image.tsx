import type { ImageProps } from "../../props/generation/Image";
/** PDF Image primitive. Used as a description inside an output component, never as HTML. */
const Image = (_props: ImageProps) => null;
export default Image;
