import type { RectProps } from "../../props/generation/Rect";
/** PDF Rect primitive. Used as a description inside an output component, never as HTML. */
const Rect = (_props: RectProps) => null;
export default Rect;
