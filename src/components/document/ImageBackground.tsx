import type { ImageBackgroundProps } from "../../props/generation/ImageBackground";
/** PDF ImageBackground primitive. Used as a description inside an output component, never as HTML. */
const ImageBackground = (_props: ImageBackgroundProps) => null;
export default ImageBackground;
