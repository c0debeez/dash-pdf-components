import type { PolylineProps } from "../../props/generation/Polyline";
/** PDF Polyline primitive. Used as a description inside an output component, never as HTML. */
const Polyline = (_props: PolylineProps) => null;
export default Polyline;
