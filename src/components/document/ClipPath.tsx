import type { ClipPathProps } from "../../props/generation/ClipPath";
/** PDF ClipPath primitive. Used as a description inside an output component, never as HTML. */
const ClipPath = (_props: ClipPathProps) => null;
export default ClipPath;
