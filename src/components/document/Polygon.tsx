import type { PolygonProps } from "../../props/generation/Polygon";
/** PDF Polygon primitive. Used as a description inside an output component, never as HTML. */
const Polygon = (_props: PolygonProps) => null;
export default Polygon;
