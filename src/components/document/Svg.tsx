import type { SvgProps } from "../../props/generation/Svg";
/** PDF Svg primitive. Used as a description inside an output component, never as HTML. */
const Svg = (_props: SvgProps) => null;
export default Svg;
