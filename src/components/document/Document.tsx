import type { DocumentProps } from "../../props/generation/Document";
/** PDF Document primitive. Used as a description inside an output component, never as HTML. */
const Document = (_props: DocumentProps) => null;
export default Document;
