import type { CanvasProps } from "../../props/generation/Canvas";
/** PDF Canvas primitive. Used as a description inside an output component, never as HTML. */
const Canvas = (_props: CanvasProps) => null;
export default Canvas;
