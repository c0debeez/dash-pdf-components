import type { ListProps } from "../../props/generation/List";
/** PDF List primitive. Used as a description inside an output component, never as HTML. */
const List = (_props: ListProps) => null;
export default List;
