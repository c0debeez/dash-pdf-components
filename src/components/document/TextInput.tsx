import type { TextInputProps } from "../../props/generation/TextInput";
/** PDF TextInput primitive. Used as a description inside an output component, never as HTML. */
const TextInput = (_props: TextInputProps) => null;
export default TextInput;
