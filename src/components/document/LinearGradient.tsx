import type { LinearGradientProps } from "../../props/generation/LinearGradient";
/** PDF LinearGradient primitive. Used as a description inside an output component, never as HTML. */
const LinearGradient = (_props: LinearGradientProps) => null;
export default LinearGradient;
