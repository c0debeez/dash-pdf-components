import type { ViewProps } from "../../props/generation/View";
/** PDF View primitive. Used as a description inside an output component, never as HTML. */
const View = (_props: ViewProps) => null;
export default View;
