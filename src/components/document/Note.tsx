import type { NoteProps } from "../../props/generation/Note";
/** PDF Note primitive. Used as a description inside an output component, never as HTML. */
const Note = (_props: NoteProps) => null;
export default Note;
