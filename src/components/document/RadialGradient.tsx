import type { RadialGradientProps } from "../../props/generation/RadialGradient";
/** PDF RadialGradient primitive. Used as a description inside an output component, never as HTML. */
const RadialGradient = (_props: RadialGradientProps) => null;
export default RadialGradient;
