import type { DefsProps } from "../../props/generation/Defs";
/** PDF Defs primitive. Used as a description inside an output component, never as HTML. */
const Defs = (_props: DefsProps) => null;
export default Defs;
