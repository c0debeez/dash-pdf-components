import type { EllipseProps } from "../../props/generation/Ellipse";
/** PDF Ellipse primitive. Used as a description inside an output component, never as HTML. */
const Ellipse = (_props: EllipseProps) => null;
export default Ellipse;
