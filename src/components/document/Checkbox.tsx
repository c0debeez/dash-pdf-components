import type { CheckboxProps } from "../../props/generation/Checkbox";
/** PDF Checkbox primitive. Used as a description inside an output component, never as HTML. */
const Checkbox = (_props: CheckboxProps) => null;
export default Checkbox;
