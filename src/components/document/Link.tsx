import type { LinkProps } from "../../props/generation/Link";
/** PDF Link primitive. Used as a description inside an output component, never as HTML. */
const Link = (_props: LinkProps) => null;
export default Link;
