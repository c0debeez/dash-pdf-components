import type { StopProps } from "../../props/generation/Stop";
/** PDF Stop primitive. Used as a description inside an output component, never as HTML. */
const Stop = (_props: StopProps) => null;
export default Stop;
