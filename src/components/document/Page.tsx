import type { PageProps } from "../../props/generation/Page";
/** PDF Page primitive. Used as a description inside an output component, never as HTML. */
const Page = (_props: PageProps) => null;
export default Page;
