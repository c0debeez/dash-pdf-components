import type { LineProps } from "../../props/generation/Line";
/** PDF Line primitive. Used as a description inside an output component, never as HTML. */
const Line = (_props: LineProps) => null;
export default Line;
