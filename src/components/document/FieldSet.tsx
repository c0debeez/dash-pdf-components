import type { FieldSetProps } from "../../props/generation/FieldSet";
/** PDF FieldSet primitive. Used as a description inside an output component, never as HTML. */
const FieldSet = (_props: FieldSetProps) => null;
export default FieldSet;
