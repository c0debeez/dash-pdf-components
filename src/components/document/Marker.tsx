import type { MarkerProps } from "../../props/generation/Marker";
/** PDF Marker primitive. Used as a description inside an output component, never as HTML. */
const Marker = (_props: MarkerProps) => null;
export default Marker;
