import type { TextProps } from "../../props/generation/Text";
/** PDF Text primitive. Used as a description inside an output component, never as HTML. */
const Text = (_props: TextProps) => null;
export default Text;
