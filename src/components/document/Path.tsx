import type { PathProps } from "../../props/generation/Path";
/** PDF Path primitive. Used as a description inside an output component, never as HTML. */
const Path = (_props: PathProps) => null;
export default Path;
