import type { CircleProps } from "../../props/generation/Circle";
/** PDF Circle primitive. Used as a description inside an output component, never as HTML. */
const Circle = (_props: CircleProps) => null;
export default Circle;
