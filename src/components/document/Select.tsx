import type { SelectProps } from "../../props/generation/Select";
/** PDF Select primitive. Used as a description inside an output component, never as HTML. */
const Select = (_props: SelectProps) => null;
export default Select;
