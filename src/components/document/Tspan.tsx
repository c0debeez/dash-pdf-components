import type { TspanProps } from "../../props/generation/Tspan";
/** PDF Tspan primitive. Used as a description inside an output component, never as HTML. */
const Tspan = (_props: TspanProps) => null;
export default Tspan;
